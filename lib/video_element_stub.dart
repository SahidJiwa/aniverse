// video_element_stub.dart — Mobile / Desktop stub
// No-op implementation so the app compiles on non-web platforms.

void registerVideoElement(String url, String viewId) {
  // Not supported on this platform — YouTube player is used instead.
}
